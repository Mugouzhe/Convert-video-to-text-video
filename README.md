# Convert-video-to-text-video
将视频转化为文字视频
此程序使用了人机交互界面，方便选择和设置。说一下其转换原理，先输入文字作为图片像素，将视频的每一帧转化为灰度图，再把灰度图里面的每一个像素值找出其与之匹配的文字。在此之前已经计算好每个字的灰度并排序，然后将排序好的文字映射到灰度图的每一个像素。可以知道，灰度越深的像素与之拼配的文字笔划一般也就越多，从而在整体上呈现出灰度图像。
